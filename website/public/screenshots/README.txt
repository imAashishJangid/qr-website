Drop the 3 vendor app screenshots in this folder with these exact filenames:

  dashboard.png   -> Vendor Dashboard screen (stats + Tables & QR codes)
  orders.png      -> Vendor Orders screen (live order queue)
  profile.png     -> Vendor Profile screen (sales summary + menu breakdown)

They are referenced directly from src/components/Screenshots.jsx as /screenshots/<filename>.
This README is just a placeholder note and can be deleted once the real images are added.

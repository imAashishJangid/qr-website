# Leave empty for local dev (uses Vite's /api proxy to http://localhost:8087)
# For production (e.g. Netlify), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
